from itertools import permutations

# Input string
input_string = "abc"

# Generate all permutations
all_permutations = [''.join(p) for p in permutations(input_string)]

# Display the results
print("All permutations of the string:", all_permutations)


output:
=========
All permutations of the string: ['abc', 'acb', 'bac', 'bca', 'cab', 'cba']
